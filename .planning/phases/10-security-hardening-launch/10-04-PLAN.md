---
phase: 10-security-hardening-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "Every list ViewSet that feeds an AG Grid is documented with its querysets, select_related, and prefetch_related"
    - "Any N+1 risk on list endpoints is flagged with the file:line and the recommended `.select_related()` / `.prefetch_related()` fix"
    - "Every hot-path model field used in filters/sorts is checked against migrations for an index; missing indexes are listed"
    - "Pagination is confirmed on every AG Grid list endpoint (PERF-02)"
  artifacts:
    - path: ".planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md"
      provides: "Performance findings covering PERF-01 and PERF-02"
      contains: "N+1"
  key_links:
    - from: "AG Grid list endpoints"
      to: "ORM querysets"
      via: "select_related / prefetch_related coverage"
      pattern: "select_related|prefetch_related"
---

<objective>
Produce a performance audit that validates PERF-01 (3G <2s dashboard, <1s customer list, <500ms filter) and PERF-02 (server-side pagination everywhere), and lists any code-level fixes needed before Lighthouse testing in 10-07.

Purpose: Before running expensive browser-based performance tests, close the cheap wins (missing indexes, N+1 queries). This is the code-side companion to the human-run Lighthouse test in 10-07.

Output: `.planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md` with a queryset/index matrix and an ordered fix list. Code changes are NOT made here — they are either trivial follow-ups the user triggers, or deferred to post-launch.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit ORM hot paths and index coverage</name>
  <files>.planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md</files>
  <action>
  Produce `.planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md` containing:

  1) List every list ViewSet that backs an AG Grid. Grep: `grep -rn "class .*ListView\|ListAPIView\|ModelViewSet\|ReadOnlyModelViewSet" backend/ --include="*.py"`. For each hit, open the file and record:
     - File:line
     - Model name
     - queryset expression (exact line)
     - select_related / prefetch_related usage — if absent but the serializer traverses a FK, mark as `N+1 RISK`
     - pagination_class — if overridden to None, mark as `PAGINATION MISSING` (blocker for PERF-02)

  2) Index coverage matrix. For each model used in the ViewSets above (customers.Customer, jobs.Job, approvals.Approval, accounts.Invoice, accounts.Payment, etc.):
     - Grep the model file for `db_index=True`, `Meta.indexes`, `unique=True`
     - Grep the frontend and ViewSet for which fields are used in `.filter()`, `.order_by()`, AG Grid filterModel keys, AG Grid sortModel keys
     - Any field used for filter/sort that is NOT indexed = a `MISSING INDEX` row
     - Commands to help: `grep -rn "db_index" backend/ --include="models.py"`, `grep -rn "class Meta" -A 5 backend/ --include="models.py"`

  3) Spot-check for N+1 in serializers. Grep: `grep -rn "SerializerMethodField\|source=" backend/ --include="serializers.py"`. For each match, confirm the underlying queryset in the ViewSet preloads the relation.

  4) Pagination confirmation (PERF-02). `grep -rn "pagination_class\s*=" backend/ --include="*.py"`. List each override. Confirm the global default (`rest_framework.pagination.PageNumberPagination`, PAGE_SIZE=20) from settings.py applies everywhere else.

  5) Static/media delivery. Confirm `STATIC_ROOT` exists in settings and note the deployment will need `python manage.py collectstatic`. Flag the need for a `whitenoise` addition ONLY if the deployment target serves Django directly without Nginx in front — in the common case (Railway/Render with their built-in CDN, or Nginx) this is acceptable as-is, just document it.

  Output structure:
  ```markdown
  # Phase 10 Performance Audit

  **Date:** {today}
  **Scope:** PERF-01 (page speed targets), PERF-02 (server-side pagination)

  ## List Endpoints (AG Grid feeders)

  | File:line | Model | select_related | prefetch_related | pagination | N+1 risk |
  |-----------|-------|----------------|------------------|------------|----------|
  | ... | Customer | customer_type | None | default (20) | LOW |

  ## Index Coverage

  | Model.field | Filter/Sort usage | Indexed? | Action |
  |-------------|-------------------|----------|--------|
  | ... | ... | ... | ... |

  ## Pagination (PERF-02)

  Default: PageNumberPagination / PAGE_SIZE=20 via settings.REST_FRAMEWORK.
  Overrides found:
  - (list any)

  ## Findings

  ### BLOCKERS (must fix before launch)
  - _None_ / ...

  ### RECOMMENDATIONS (fix before Lighthouse)
  1. Add `select_related('customer', 'approver')` to `backend/jobs/views.py:42` — currently N+1 when grid expands customer_name.
  2. Add `db_index=True` to `Customer.email` — used as AG Grid filter.

  ### DEFERRED (post-launch)
  - ...

  ## Static Delivery

  Note on whether the deployment target serves static files via Nginx/CDN or needs Whitenoise.
  ```

  Do NOT modify any backend source files in this plan. The fix list is the artifact; the execution of those fixes is an optional downstream follow-up (or happens manually).
  </action>
  <verify>
  - File exists: `test -f .planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md`
  - Contains the required sections: `for s in "List Endpoints" "Index Coverage" "Pagination" "BLOCKERS" "RECOMMENDATIONS"; do grep -q "$s" .planning/phases/10-security-hardening-launch/10-04-PERF-AUDIT.md || echo "MISSING: $s"; done` prints nothing.
  - No backend files modified: `git status --short backend/ | grep -q "^ M\|^M " && echo "UNEXPECTED CHANGES" || echo "clean"` prints `clean`.
  </verify>
  <done>
  - PERF-AUDIT.md written with queryset, index, and pagination matrices
  - Blocker and recommendation lists are explicit
  - No code changes made
  </done>
</task>

</tasks>

<verification>
1. Every AG Grid list endpoint is in the matrix.
2. Every filtered/sorted field has an index verdict.
3. Any missing pagination is flagged as a blocker.
</verification>

<success_criteria>
- [ ] All list endpoints enumerated
- [ ] N+1 risks explicitly flagged with file:line
- [ ] Index matrix present
- [ ] PERF-02 pagination verified endpoint-by-endpoint
- [ ] Static file delivery path documented
</success_criteria>

<output>
After completion, create `.planning/phases/10-security-hardening-launch/10-04-SUMMARY.md` AND leave `10-04-PERF-AUDIT.md` alongside.
</output>
