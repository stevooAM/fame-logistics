---
phase: 03-administration-lookup-setup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/setup/models.py
  - backend/setup/serializers.py
  - backend/setup/views.py
  - backend/setup/urls.py
  - backend/setup/admin.py
  - backend/config/urls.py
  - backend/setup/migrations/0002_add_sort_order_and_code_fields.py
  - backend/setup/management/commands/seed_lookups.py
autonomous: true

must_haves:
  truths:
    - "All five lookup tables (Port, CargoType, Currency, DocumentType, CompanyProfile) have CRUD API endpoints"
    - "Each lookup entry has name, code/abbreviation, and sort_order fields"
    - "Deactivated lookup values are excluded from default list queries (only active=true returned by default)"
    - "Lookup values support soft-delete (deactivate) — no hard deletes"
    - "Pre-seeded defaults exist: GHS/USD/GBP/EUR currencies and common cargo types"
    - "CompanyProfile is a singleton — only one record exists"
  artifacts:
    - path: "backend/setup/serializers.py"
      provides: "Serializers for all lookup models"
      contains: "PortSerializer"
    - path: "backend/setup/views.py"
      provides: "ViewSets or APIViews for all lookup CRUD"
      contains: "PortViewSet"
    - path: "backend/setup/urls.py"
      provides: "URL routes for all lookup endpoints"
      contains: "ports"
    - path: "backend/setup/management/commands/seed_lookups.py"
      provides: "Management command to seed default lookup values"
      contains: "seed_lookups"
  key_links:
    - from: "backend/setup/views.py"
      to: "setup.models"
      via: "Django ORM CRUD"
      pattern: "queryset.*objects"
    - from: "backend/config/urls.py"
      to: "setup.urls"
      via: "include"
      pattern: "include.*setup\\.urls"
---

<objective>
Build Lookup Table CRUD APIs: model updates (add sort_order/code fields), serializers, viewsets, URL routing, and seed data for all five lookup tables plus company profile.

Purpose: SETUP-01 through SETUP-05 require configurable reference data. These APIs serve both the admin lookup UI (Plan 07) and downstream modules that need dropdown values.
Output: Working CRUD endpoints for ports, cargo types, currencies, document types, and company profile. Seed command for default data.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/setup/models.py
@backend/config/urls.py
@backend/core/permissions.py
@backend/core/audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update lookup models and create migration</name>
  <files>backend/setup/models.py, backend/setup/migrations/0002_add_sort_order_and_code_fields.py</files>
  <action>
Update the existing setup/models.py to add missing fields per user decisions:

1. **Port** — already has name, code, country, is_active. ADD:
   - `sort_order = models.IntegerField(default=0)` — controls dropdown display order
   - Update Meta: `ordering = ["sort_order", "name"]`

2. **CargoType** — already has name, description, is_active. ADD:
   - `code = models.CharField(max_length=20, unique=True, blank=True, default="")` — abbreviation (e.g., "GEN", "BLK")
   - `sort_order = models.IntegerField(default=0)`
   - Update Meta: `ordering = ["sort_order", "name"]`

3. **Currency** — already has code, name, symbol, exchange_rate, is_default, is_active. ADD:
   - `sort_order = models.IntegerField(default=0)`
   - Update Meta: `ordering = ["sort_order", "code"]`

4. **DocumentType** — already has name, description, is_active. ADD:
   - `code = models.CharField(max_length=20, unique=True, blank=True, default="")` — abbreviation (e.g., "BL", "INV")
   - `sort_order = models.IntegerField(default=0)`
   - Update Meta: `ordering = ["sort_order", "name"]`

5. **CompanyProfile** — already has name, logo_url, email, phone, address, website, tin. ADD:
   - `registration_number = models.CharField(max_length=100, blank=True)` — business registration number
   - `logo = models.ImageField(upload_to="company/", blank=True, null=True)` — actual file upload (keep logo_url for backward compat)
   - Note: logo file upload requires Pillow in requirements.txt — add it if not already present

Create migration file manually (since Docker CLI is unavailable):
- `backend/setup/migrations/0002_add_sort_order_and_code_fields.py`
- Add sort_order to Port, CargoType, Currency, DocumentType
- Add code to CargoType, DocumentType
- Add registration_number and logo to CompanyProfile
- Use `migrations.AddField` operations

Also add Pillow to backend/requirements.txt if not present.
  </action>
  <verify>`cd backend && python -c "from setup.models import Port, CargoType, Currency, DocumentType, CompanyProfile; print('Models import OK')"` and verify migration file exists: `ls backend/setup/migrations/0002_*.py`</verify>
  <done>All lookup models updated with sort_order and code fields. Migration file created. CompanyProfile has logo upload field.</done>
</task>

<task type="auto">
  <name>Task 2: Lookup CRUD serializers, views, URLs, and seed command</name>
  <files>backend/setup/serializers.py, backend/setup/views.py, backend/setup/urls.py, backend/config/urls.py, backend/setup/management/commands/seed_lookups.py</files>
  <action>
**backend/setup/serializers.py** — Create serializers for all lookup models:

1. **PortSerializer** — ModelSerializer, all fields. Validate name and code uniqueness.
2. **CargoTypeSerializer** — ModelSerializer, all fields. Validate name and code uniqueness.
3. **CurrencySerializer** — ModelSerializer, all fields. Validate code uniqueness.
4. **DocumentTypeSerializer** — ModelSerializer, all fields. Validate name and code uniqueness.
5. **CompanyProfileSerializer** — ModelSerializer, all fields. logo field as ImageField (optional).

**backend/setup/views.py** — Create ViewSets using DRF ModelViewSet:

For each lookup (Port, CargoType, Currency, DocumentType):
- Use ModelViewSet with permission_classes=[IsAdmin]
- Default queryset: filter is_active=True (so downstream dropdowns only get active values)
- Add ?include_inactive=true query param to include deactivated entries (for admin UI)
- Override destroy() to soft-delete: set is_active=False instead of deleting. Return 200 with updated object (not 204).
- Include AuditLogMixin from core.audit for auto-logging CRUD
- Pagination: not needed for lookups (small datasets <100 entries typically)

For CompanyProfile:
- **CompanyProfileView** (APIView, permission_classes=[IsAdmin]):
  - GET /api/setup/company-profile/ — return the singleton profile (create with defaults if none exists)
  - PATCH /api/setup/company-profile/ — update the singleton
  - Handle logo file upload via multipart/form-data (not JSON)
  - Include audit logging

For downstream dropdown consumption (all roles can read active lookups):
- **LookupDropdownView** (APIView, permission_classes=[IsAnyRole]):
  - GET /api/setup/dropdowns/ — returns all active lookups in one response:
    ```json
    {
      "ports": [...],
      "cargo_types": [...],
      "currencies": [...],
      "document_types": [...]
    }
    ```
  - This single endpoint avoids 4 separate API calls when loading a form

**backend/setup/urls.py** — Create URL config:
```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

router = DefaultRouter()
router.register(r'ports', views.PortViewSet, basename='port')
router.register(r'cargo-types', views.CargoTypeViewSet, basename='cargo-type')
router.register(r'currencies', views.CurrencyViewSet, basename='currency')
router.register(r'document-types', views.DocumentTypeViewSet, basename='document-type')

urlpatterns = [
    path('', include(router.urls)),
    path('company-profile/', views.CompanyProfileView.as_view(), name='company-profile'),
    path('dropdowns/', views.LookupDropdownView.as_view(), name='lookup-dropdowns'),
]
```

**backend/config/urls.py** — Add setup URLs:
```python
path("api/setup/", include("setup.urls")),
```

**backend/setup/management/commands/seed_lookups.py** — Management command:
- Create directories: `backend/setup/management/__init__.py`, `backend/setup/management/commands/__init__.py`
- Command: `python manage.py seed_lookups`
- Seeds (idempotent — skip if already exists):
  - Currencies: GHS (Ghana Cedi, GH₵), USD (US Dollar, $), GBP (British Pound, £), EUR (Euro, €). Set GHS as is_default=True.
  - Cargo types: General Cargo (GEN), Bulk Cargo (BLK), Containerized (CTN), Perishables (PER), Hazardous (HAZ), Vehicles (VEH), Liquid Bulk (LQD)
  - Ports: start empty (per user decision)
  - Document types: start empty (per user decision)
  - CompanyProfile: create singleton with name="Fame Logistics" if none exists
  </action>
  <verify>`cd backend && python -c "from setup.views import PortViewSet, CargoTypeViewSet, CurrencyViewSet, DocumentTypeViewSet, CompanyProfileView, LookupDropdownView; print('Views OK')"` and `cd backend && python -c "from setup.urls import urlpatterns; print(f'{len(urlpatterns)} URL patterns')"` and `ls backend/setup/management/commands/seed_lookups.py`</verify>
  <done>All lookup CRUD endpoints registered under /api/setup/. Soft-delete enforced. Dropdown endpoint available. Seed command created with currencies and cargo types.</done>
</task>

</tasks>

<verification>
- All 4 lookup ViewSets registered via router under /api/setup/
- CompanyProfile singleton endpoint at /api/setup/company-profile/
- Dropdown aggregation endpoint at /api/setup/dropdowns/
- Soft-delete (deactivation) works on all lookup ViewSets
- AuditLogMixin logs all CRUD operations
- seed_lookups command creates default currencies and cargo types
- Migration file applies cleanly
</verification>

<success_criteria>
All five lookup tables have working CRUD APIs. Deactivated values are excluded from default queries. Pre-seeded currencies (GHS, USD, GBP, EUR) and cargo types exist. CompanyProfile is a singleton. Downstream modules can fetch all active lookups via /api/setup/dropdowns/.
</success_criteria>

<output>
After completion, create `.planning/phases/03-administration-lookup-setup/03-06-SUMMARY.md`
</output>
