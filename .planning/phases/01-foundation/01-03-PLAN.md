---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/next.config.js
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/globals.css
  - frontend/src/lib/utils.ts
  - frontend/components.json
  - frontend/Dockerfile
  - frontend/.eslintrc.json
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts at localhost:3000 without errors"
    - "TypeScript compilation passes with no errors"
    - "Tailwind CSS classes apply correctly in the browser"
    - "shadcn/ui components can be imported and rendered"
    - "AG Grid renders a table with mock data"
  artifacts:
    - path: "frontend/package.json"
      provides: "All frontend dependencies"
      contains: "ag-grid-react"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind configuration with design tokens"
      contains: "theme"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout"
      contains: "RootLayout"
    - path: "frontend/components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "frontend/tailwind.config.ts"
      to: "frontend/src/app/globals.css"
      via: "Tailwind directives"
      pattern: "@tailwind"
---

<objective>
Set up the Next.js 14 App Router project with TypeScript, Tailwind CSS, shadcn/ui, and AG Grid Community.

Purpose: The frontend application shell that all UI phases build upon. Installs and verifies all frontend dependencies so subsequent plans can focus on features, not setup.
Output: A running Next.js dev server with Tailwind, shadcn/ui, and AG Grid all wired and rendering.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js project initialization with all dependencies</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/next.config.js
    frontend/postcss.config.js
    frontend/tailwind.config.ts
    frontend/src/app/layout.tsx
    frontend/src/app/page.tsx
    frontend/src/app/globals.css
    frontend/src/lib/utils.ts
    frontend/components.json
    frontend/.eslintrc.json
    frontend/Dockerfile
  </files>
  <action>
    Initialize the Next.js 14 project inside `frontend/` using App Router with `src/` directory.

    Run `npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` inside the frontend directory (or manually create the equivalent structure).

    Install additional dependencies:
    ```bash
    npm install ag-grid-react ag-grid-community react-hook-form @hookform/resolvers zod zustand lucide-react
    npm install -D @types/node
    ```

    Initialize shadcn/ui:
    ```bash
    npx shadcn-ui@latest init
    ```
    Configure shadcn/ui (components.json):
    - Style: "default"
    - Base color: "slate" (we override with brand colours)
    - CSS variables: true
    - Tailwind config path: tailwind.config.ts
    - Components alias: @/components
    - Utils alias: @/lib/utils

    Add initial shadcn/ui components (per CONTEXT.md locked decision):
    ```bash
    npx shadcn-ui@latest add button input table dialog badge
    ```
    Note: shadcn/ui uses "dialog" not "modal" — this satisfies the "Modal" requirement from CONTEXT.md.

    Configure `frontend/tailwind.config.ts` with Fame FMS design tokens (per CONTEXT.md locked decision):
    ```typescript
    // Extend theme with brand colours
    colors: {
      brand: {
        teal: '#1F7A8C',
        amber: '#F89C1C',
        navy: '#2B3E50',
        white: '#FFFFFF',
        text: '#333333',
      }
    },
    // Spacing scale: 4/8/12/16/24/32 (Tailwind defaults cover these)
    // Border radius presets
    borderRadius: {
      'sm': '4px',
      'md': '8px',
      'lg': '12px',
    },
    // Typography scale will be defined as Tailwind utilities
    // Elevation shadows for cards and modals
    boxShadow: {
      'card': '0 1px 3px rgba(0,0,0,0.12), 0 1px 2px rgba(0,0,0,0.06)',
      'modal': '0 10px 25px rgba(0,0,0,0.15), 0 6px 10px rgba(0,0,0,0.10)',
    },
    ```

    Set up `frontend/src/app/globals.css` with:
    - Tailwind directives (@tailwind base, components, utilities)
    - CSS custom properties for brand colours (for shadcn/ui theming)
    - Typography scale classes (h1 through caption sizes)

    Create `frontend/src/lib/utils.ts` with the `cn()` utility (clsx + tailwind-merge) — shadcn/ui init typically creates this.

    Update `frontend/src/app/layout.tsx`:
    - Import globals.css
    - Set metadata: title "Fame FMS", description "Fame Logistics Freight Management System"
    - Use Inter font from next/font/google
    - Minimal shell — just the HTML structure

    Update `frontend/src/app/page.tsx`:
    - Temporary landing page that imports and renders a shadcn Button and Badge to verify they work
    - Render a basic AG Grid table with 5 hardcoded mock rows (use a simple data set like freight shipment entries with columns: Job No, Customer, Type, Status, Date)
    - This page is temporary — Plan 04 will replace it with the proper login page

    Update `frontend/.eslintrc.json` to extend next/core-web-vitals.

    Update `frontend/Dockerfile` to properly install node_modules and run `npm run dev`.

    Update `frontend/next.config.js`:
    - output: 'standalone' (for Docker deployment)
    - No other special config needed yet
  </action>
  <verify>
    Run `cd frontend && npm run build` — should complete with no TypeScript errors.
    Run `cd frontend && npx tsc --noEmit` — should pass with no errors.
    Run `docker compose up frontend -d` — Next.js starts at localhost:3000.
    Visit http://localhost:3000 — page renders with shadcn Button, Badge, and AG Grid table visible.
  </verify>
  <done>Next.js 14 App Router runs in Docker. TypeScript compiles clean. Tailwind CSS applies brand colours. shadcn/ui Button, Input, Table, Dialog, and Badge are installed and importable. AG Grid renders a demo table with 5 mock rows. ESLint is configured.</done>
</task>

</tasks>

<verification>
- `docker compose up frontend -d` starts Next.js at localhost:3000
- `cd frontend && npx tsc --noEmit` passes
- `cd frontend && npm run build` succeeds
- Browser at localhost:3000 shows shadcn/ui components and AG Grid table
- `ls frontend/src/components/ui/` shows button.tsx, input.tsx, table.tsx, dialog.tsx, badge.tsx
</verification>

<success_criteria>
Next.js 14 runs in Docker with TypeScript, Tailwind CSS (with brand colour tokens), shadcn/ui (5 components installed), and AG Grid Community all wired and rendering correctly in a browser.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
