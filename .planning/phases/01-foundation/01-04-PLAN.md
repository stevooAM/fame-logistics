---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/app/(dashboard)/layout.tsx
  - frontend/src/app/(dashboard)/page.tsx
  - frontend/src/components/layout/sidebar.tsx
  - frontend/src/components/layout/sidebar-nav.tsx
  - frontend/src/components/layout/sidebar-user.tsx
  - frontend/src/components/demo/ag-grid-demo.tsx
  - frontend/src/lib/navigation.ts
autonomous: false

must_haves:
  truths:
    - "Login page renders with teal (#1F7A8C), amber (#F89C1C), and dark navy (#2B3E50) brand colours"
    - "Login page has company logo placeholder, username field, password field, and submit button"
    - "Sidebar shows all 7 nav items: Dashboard, Customers, Jobs, Approvals, Accounts, Reports, Admin"
    - "Sidebar collapses between full labels and icon-only mode"
    - "Sidebar bottom shows user name placeholder, role badge, and logout button"
    - "Each nav item links to a placeholder page"
    - "AG Grid demo table with 5-10 mock rows renders on the Dashboard placeholder"
  artifacts:
    - path: "frontend/src/app/login/page.tsx"
      provides: "Login page with brand design"
      contains: "#1F7A8C"
    - path: "frontend/src/components/layout/sidebar.tsx"
      provides: "Collapsible sidebar shell"
      contains: "collapsed"
    - path: "frontend/src/lib/navigation.ts"
      provides: "Navigation config with role filtering stub"
      contains: "roles"
    - path: "frontend/src/components/demo/ag-grid-demo.tsx"
      provides: "AG Grid demo table"
      contains: "AgGridReact"
  key_links:
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/layout/sidebar.tsx"
      via: "Layout imports sidebar"
      pattern: "import.*[Ss]idebar"
    - from: "frontend/src/components/layout/sidebar-nav.tsx"
      to: "frontend/src/lib/navigation.ts"
      via: "Navigation items config"
      pattern: "import.*navigation"
    - from: "frontend/src/app/(dashboard)/page.tsx"
      to: "frontend/src/components/demo/ag-grid-demo.tsx"
      via: "Dashboard imports AgGridDemo"
      pattern: "import.*[Aa]g[Gg]rid[Dd]emo"
---

<objective>
Build the login page with full brand design, the collapsible sidebar layout shell with all navigation items, placeholder pages for each module, and the AG Grid demo table.

Purpose: Delivers the two visible UI artifacts from Phase 1 success criteria: the branded login page and the sidebar shell. Also wires the AG Grid demo table per CONTEXT.md requirements.
Output: A login page matching brand colours, a collapsible sidebar with 7 nav items (role-filtering stubbed), placeholder pages, and an AG Grid demo.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page with brand design</name>
  <files>
    frontend/src/app/login/page.tsx
    frontend/src/app/page.tsx
    frontend/src/app/layout.tsx
  </files>
  <action>
    Create `frontend/src/app/login/page.tsx` — the fully designed login page per CONTEXT.md locked decision ("fully designed with brand colours, not a placeholder").

    Layout:
    - Full-screen centered layout with dark navy (#2B3E50) background
    - Centered white card (max-w-md) with rounded corners and modal-level shadow
    - Company logo placeholder at the top (a styled div with "FAME LOGISTICS" text in teal, or an img tag with a placeholder src — design should look professional even without a real logo)
    - Tagline or subtitle: "Freight Management System" in muted text below logo

    Form (using shadcn/ui Input and Button components):
    - Username field: shadcn Input with label, placeholder "Enter your username"
    - Password field: shadcn Input type="password" with label, placeholder "Enter your password"
    - "Sign In" button: full-width, styled with brand teal (#1F7A8C) background, white text, hover state with slightly darker teal
    - Form is NOT functional yet (no API call) — just the visual design. Phase 2 wires auth.
    - Use React Hook Form + Zod for form validation even now (validates username required, password required min 6 chars) to establish the pattern
    - On submit, just console.log the values for now

    Colour usage:
    - Background: dark navy #2B3E50
    - Primary button: teal #1F7A8C
    - Accent elements (logo underline, focus rings): amber #F89C1C
    - Card: white #FFFFFF
    - Text: #333333

    Update `frontend/src/app/page.tsx`: Replace the temporary AG Grid demo page from 01-03 with a redirect to /login. Use `redirect('/login')` from next/navigation. The 01-03 temporary page.tsx is no longer needed — this plan takes ownership of the root page.

    Update `frontend/src/app/layout.tsx` if needed to ensure clean full-page layout for the login route (no sidebar on login).
  </action>
  <verify>
    Visit http://localhost:3000/login — login page renders with correct brand colours.
    Inspect elements: background is #2B3E50, button is #1F7A8C, accent uses #F89C1C.
    Form validation fires on empty submit (shows required errors).
    Visit http://localhost:3000/ — redirects to /login.
  </verify>
  <done>Login page renders with dark navy background, white card, teal sign-in button, amber accents, logo placeholder, and form fields. React Hook Form + Zod validation is wired. Root page.tsx redirects to /login (replacing the 01-03 temporary demo page). Page matches brand colours exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar layout shell, navigation, AG Grid demo, and placeholder pages</name>
  <files>
    frontend/src/components/layout/sidebar.tsx
    frontend/src/components/layout/sidebar-nav.tsx
    frontend/src/components/layout/sidebar-user.tsx
    frontend/src/components/demo/ag-grid-demo.tsx
    frontend/src/lib/navigation.ts
    frontend/src/app/(dashboard)/layout.tsx
    frontend/src/app/(dashboard)/page.tsx
    frontend/src/app/(dashboard)/customers/page.tsx
    frontend/src/app/(dashboard)/jobs/page.tsx
    frontend/src/app/(dashboard)/approvals/page.tsx
    frontend/src/app/(dashboard)/accounts/page.tsx
    frontend/src/app/(dashboard)/reports/page.tsx
    frontend/src/app/(dashboard)/admin/page.tsx
  </files>
  <action>
    Create `frontend/src/lib/navigation.ts` — navigation configuration:
    ```typescript
    type NavItem = {
      label: string;
      href: string;
      icon: string; // Lucide icon name
      roles: string[]; // Roles that can see this item
      badge?: number; // For approval count (Phase 6)
    };

    export const navItems: NavItem[] = [
      { label: 'Dashboard', href: '/', icon: 'LayoutDashboard', roles: ['admin', 'manager', 'operations', 'finance', 'viewer'] },
      { label: 'Customers', href: '/customers', icon: 'Users', roles: ['admin', 'manager', 'operations', 'viewer'] },
      { label: 'Jobs', href: '/jobs', icon: 'Package', roles: ['admin', 'manager', 'operations', 'viewer'] },
      { label: 'Approvals', href: '/approvals', icon: 'CheckCircle', roles: ['admin', 'manager'] },
      { label: 'Accounts', href: '/accounts', icon: 'Receipt', roles: ['admin', 'manager', 'finance'] },
      { label: 'Reports', href: '/reports', icon: 'BarChart3', roles: ['admin', 'manager', 'finance', 'viewer'] },
      { label: 'Admin', href: '/admin', icon: 'Settings', roles: ['admin'] },
    ];

    // Stub role filtering — Phase 2 will activate this with real user roles
    export function filterNavByRole(items: NavItem[], userRole?: string): NavItem[] {
      if (!userRole) return items; // Show all when no role (dev mode)
      return items.filter(item => item.roles.includes(userRole));
    }
    ```

    Create `frontend/src/components/layout/sidebar.tsx`:
    - Dark navy (#2B3E50) background, full height, left-side fixed
    - Collapsible: toggle between full width (~256px with labels) and icon-only mode (~64px)
    - Toggle button at the top (chevron icon)
    - Company name "FAME FMS" at the top (collapses to just the icon/initials)
    - Contains SidebarNav and SidebarUser components
    - Use Zustand or local state for collapsed state (Zustand preferred since it's in the stack)

    Create `frontend/src/components/layout/sidebar-nav.tsx`:
    - Renders navItems from navigation.ts
    - Each item: Lucide icon + label text (label hidden when collapsed)
    - Active state: highlighted background (teal tint) for current route
    - Use `usePathname()` from next/navigation for active detection
    - Call `filterNavByRole()` but pass no role for now (shows all items in dev)

    Create `frontend/src/components/layout/sidebar-user.tsx`:
    - Pinned to bottom of sidebar
    - User avatar placeholder (circle with initials)
    - User name: "Admin User" (hardcoded placeholder)
    - Role badge: shadcn Badge component showing "Admin" in amber
    - Logout button: icon button (LogOut icon from Lucide)
    - When sidebar collapsed: show only avatar, hide name/badge, keep logout icon

    Create `frontend/src/app/(dashboard)/layout.tsx`:
    - Route group layout that wraps all dashboard pages
    - Renders Sidebar on the left + main content area on the right
    - Main content area: padding, max-width, responsive
    - This layout does NOT apply to /login (login is outside the route group)

    Create `frontend/src/components/demo/ag-grid-demo.tsx`:
    - AG Grid table component with 5-10 hardcoded mock rows
    - Columns: Job No (e.g., "FMS-2026-00001"), Customer, Type (Import/Export/Transit), Status (Draft/Pending/etc.), Date
    - Enable column sorting, filtering, and resizing
    - Use AG Grid's default theme or the Alpine theme
    - Import AG Grid CSS in this component or in globals.css

    Create `frontend/src/app/(dashboard)/page.tsx`:
    - Dashboard page that imports and renders `AgGridDemo` from `@/components/demo/ag-grid-demo`
    - IMPORTANT: This file must `import AgGridDemo from '@/components/demo/ag-grid-demo'` (or named import) and render `<AgGridDemo />` — the AG Grid demo MUST be wired via this import, not inlined

    Create 6 placeholder pages — each is minimal but shows the page name:
    - `frontend/src/app/(dashboard)/customers/page.tsx` — "Customers" heading + "Coming in Phase 4" text
    - `frontend/src/app/(dashboard)/jobs/page.tsx` — "Jobs" heading + "Coming in Phase 5"
    - `frontend/src/app/(dashboard)/approvals/page.tsx` — "Approvals" heading + "Coming in Phase 6"
    - `frontend/src/app/(dashboard)/accounts/page.tsx` — "Accounts" heading + "Coming in Phase 7"
    - `frontend/src/app/(dashboard)/reports/page.tsx` — "Reports" heading + "Coming in Phase 9"
    - `frontend/src/app/(dashboard)/admin/page.tsx` — "Administration" heading + "Coming in Phase 3"

    Each placeholder page should use a consistent template: page heading (h1), brief description, and a subtle "placeholder" indicator.
  </action>
  <verify>
    Visit http://localhost:3000/ — sidebar renders with all 7 nav items, dark navy background.
    Click sidebar toggle — sidebar collapses to icon-only mode and expands back.
    Click each nav item — navigates to the correct placeholder page.
    Dashboard page shows AG Grid table with mock data, sortable and filterable columns.
    Sidebar bottom shows user name, role badge, and logout button.
    Verify the import wiring: `grep -r "AgGridDemo" frontend/src/app/\(dashboard\)/page.tsx` returns the import line.
  </verify>
  <done>Sidebar renders with dark navy background, 7 nav items with Lucide icons, collapse toggle, user info at bottom with role badge. Navigation links work to all placeholder pages. AG Grid demo table renders on Dashboard via import from ag-grid-demo.tsx (not inlined). Role filtering is stubbed and ready for Phase 2 activation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Login page with brand design and sidebar layout shell with navigation</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000/login — verify the login page:
       - Dark navy (#2B3E50) background fills the screen
       - White card centered with "FAME LOGISTICS" branding
       - Teal (#1F7A8C) "Sign In" button
       - Amber (#F89C1C) accent elements (focus rings, decorative elements)
       - Username and password fields present
       - Try submitting empty form — validation errors appear

    2. Visit http://localhost:3000/ — verify the sidebar shell:
       - Dark navy sidebar on the left with all 7 nav items
       - Click the collapse toggle — sidebar shrinks to icon-only
       - Click each nav item — navigates to correct page
       - User name, "Admin" badge, and logout button at bottom
       - Dashboard shows AG Grid table with mock freight data
       - Table columns are sortable and filterable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Login page at /login matches brand colours (teal, amber, dark navy)
- Sidebar renders with 7 nav items (Dashboard, Customers, Jobs, Approvals, Accounts, Reports, Admin)
- Sidebar collapse toggle works (full labels to icon-only and back)
- User info section at sidebar bottom with name, role badge, logout
- AG Grid demo table on Dashboard with sortable/filterable mock data
- All placeholder pages render when navigated to
- `npm run build` still passes with no TypeScript errors
- `grep "import.*AgGridDemo" frontend/src/app/\(dashboard\)/page.tsx` confirms the wiring
</verification>

<success_criteria>
The login page visually matches brand colours and is not a placeholder. The sidebar layout shell has all 7 navigation items, collapse toggle, and user section. AG Grid demo table renders on the Dashboard via import from ag-grid-demo.tsx. Role-aware filtering is stubbed for Phase 2 activation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
