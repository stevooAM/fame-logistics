---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/manage.py
  - backend/config/__init__.py
  - backend/config/settings.py
  - backend/config/urls.py
  - backend/config/wsgi.py
  - backend/config/asgi.py
  - backend/config/celery.py
  - backend/core/__init__.py
  - backend/core/views.py
  - backend/core/urls.py
  - backend/core/management/commands/create_default_admin.py
  - backend/requirements.txt
  - backend/pytest.ini
  - backend/Dockerfile
autonomous: true

must_haves:
  truths:
    - "Django dev server starts without errors inside Docker"
    - "Health-check endpoint returns 200 with JSON status"
    - "DRF browsable API is accessible at /api/"
    - "Celery worker connects to Redis broker"
    - "create_default_admin management command creates a superuser from env vars"
  artifacts:
    - path: "backend/config/settings.py"
      provides: "Django settings with DRF, JWT, CORS, Celery config"
      contains: "REST_FRAMEWORK"
    - path: "backend/config/celery.py"
      provides: "Celery app configuration"
      contains: "celery_app"
    - path: "backend/core/views.py"
      provides: "Health-check endpoint"
      contains: "health"
    - path: "backend/core/management/commands/create_default_admin.py"
      provides: "Default admin bootstrap command"
      contains: "create_default_admin"
    - path: "backend/requirements.txt"
      provides: "Python dependencies"
      contains: "djangorestframework"
  key_links:
    - from: "backend/config/settings.py"
      to: "backend/config/celery.py"
      via: "Celery broker URL from env"
      pattern: "CELERY_BROKER_URL"
    - from: "backend/config/urls.py"
      to: "backend/core/urls.py"
      via: "URL include"
      pattern: "include.*core"
---

<objective>
Set up the Django 5 project with DRF, Celery, Redis integration, health-check endpoint, and the create_default_admin management command.

Purpose: The backend API is the foundation for all data operations. Every subsequent phase depends on Django being properly configured with DRF, database connectivity, and async task support via Celery.
Output: A running Django dev server in Docker with DRF browsable API, health-check endpoint, and Celery worker.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Django project and core configuration</name>
  <files>
    backend/manage.py
    backend/config/__init__.py
    backend/config/settings.py
    backend/config/urls.py
    backend/config/wsgi.py
    backend/config/asgi.py
    backend/config/celery.py
    backend/requirements.txt
    backend/pytest.ini
    backend/Dockerfile
  </files>
  <action>
    Initialize the Django project inside `backend/` using `config` as the project package name (so settings live at `backend/config/settings.py`).

    Update `backend/requirements.txt` with pinned versions:
    ```
    django>=5.0,<6.0
    djangorestframework>=3.15,<4.0
    djangorestframework-simplejwt>=5.3,<6.0
    django-cors-headers>=4.3,<5.0
    psycopg2-binary>=2.9,<3.0
    celery[redis]>=5.3,<6.0
    redis>=5.0,<6.0
    python-dotenv>=1.0,<2.0
    gunicorn>=21.2,<23.0
    ruff>=0.4,<1.0
    pytest>=8.0,<9.0
    pytest-django>=4.8,<5.0
    ```

    Configure `backend/config/settings.py`:
    - Load env vars using `os.environ.get()` with fallbacks (python-dotenv loaded in manage.py)
    - DATABASES: PostgreSQL using env vars (POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_HOST, POSTGRES_PORT)
    - INSTALLED_APPS: add rest_framework, corsheaders, core (the app we create)
    - MIDDLEWARE: add corsheaders.middleware.CorsMiddleware before CommonMiddleware
    - REST_FRAMEWORK: default authentication = JWTAuthentication, default permission = IsAuthenticated, default pagination = PageNumberPagination (page_size=20)
    - SIMPLE_JWT: access token lifetime = 15 min, refresh token lifetime = 7 days, rotate refresh tokens = True
    - CORS_ALLOWED_ORIGINS: from env var, default to ["http://localhost:3000"]
    - CELERY_BROKER_URL: from REDIS_URL env var
    - CELERY_RESULT_BACKEND: from REDIS_URL env var
    - AUTH_USER_MODEL: leave as default 'auth.User' for now (Phase 2 will extend it)
    - Set DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

    Configure `backend/config/celery.py`:
    - Create Celery app named 'fame_fms'
    - Auto-discover tasks from all installed apps
    - Load config from Django settings with CELERY_ prefix

    Update `backend/config/__init__.py` to import celery app:
    ```python
    from .celery import app as celery_app
    __all__ = ('celery_app',)
    ```

    Configure `backend/config/urls.py`:
    - Include admin at /admin/
    - Include core.urls at /api/
    - Include simplejwt token views at /api/auth/token/ and /api/auth/token/refresh/

    Create `backend/pytest.ini`:
    ```
    [pytest]
    DJANGO_SETTINGS_MODULE = config.settings
    python_files = tests.py test_*.py *_tests.py
    ```

    Update `backend/Dockerfile` to set WORKDIR to /app and ensure manage.py is accessible.

    Load python-dotenv in `backend/manage.py` at the top:
    ```python
    from dotenv import load_dotenv
    load_dotenv()
    ```
  </action>
  <verify>
    From project root: `docker compose up backend -d` then `docker compose exec backend python manage.py check` should return "System check identified no issues."
  </verify>
  <done>Django starts with DRF, CORS, JWT, and Celery configured. Settings read all values from environment variables. pytest is configured.</done>
</task>

<task type="auto">
  <name>Task 2: Core app with health-check and admin bootstrap</name>
  <files>
    backend/core/__init__.py
    backend/core/apps.py
    backend/core/views.py
    backend/core/urls.py
    backend/core/tests.py
    backend/core/management/__init__.py
    backend/core/management/commands/__init__.py
    backend/core/management/commands/create_default_admin.py
  </files>
  <action>
    Create the `core` Django app inside `backend/`:

    `backend/core/views.py`:
    - Create a health_check view (DRF @api_view(['GET']) with AllowAny permission) that returns:
      ```json
      {
        "status": "healthy",
        "service": "fame-fms-backend",
        "version": "0.1.0",
        "database": true/false,
        "redis": true/false
      }
      ```
    - Database check: try a simple DB query (e.g., `connection.ensure_connection()`)
    - Redis check: try `redis.from_url(settings.CELERY_BROKER_URL).ping()`
    - Catch exceptions gracefully — return status even if DB or Redis is down

    `backend/core/urls.py`:
    - Route: `health/` -> health_check view

    `backend/core/management/commands/create_default_admin.py`:
    - Django management command that reads DJANGO_SUPERUSER_USERNAME, DJANGO_SUPERUSER_EMAIL, DJANGO_SUPERUSER_PASSWORD from env vars
    - If a user with that username already exists, skip creation and print "Admin user already exists"
    - If env vars are missing, print warning and skip
    - On success, print "Default admin created: {username}"

    `backend/core/tests.py`:
    - Write a smoke test that calls the health-check endpoint and asserts 200 status
    - Write a test that verifies create_default_admin creates a superuser
    - These are the test stubs that CI will run

    `backend/core/apps.py`:
    - Standard Django AppConfig with name='core'
  </action>
  <verify>
    Run: `docker compose exec backend python manage.py migrate` (should apply initial Django migrations)
    Run: `docker compose exec backend python manage.py create_default_admin` (should create admin user)
    Run: `docker compose exec backend pytest` (smoke tests should pass)
    Curl: `curl http://localhost:8000/api/health/` should return 200 with JSON
  </verify>
  <done>Health-check returns 200 with database and Redis status. create_default_admin creates a superuser from env vars. Smoke tests pass with pytest. DRF browsable API is accessible.</done>
</task>

</tasks>

<verification>
- `docker compose up backend db redis -d` starts all three services
- `curl http://localhost:8000/api/health/` returns 200 with {"status": "healthy", ...}
- `docker compose exec backend python manage.py create_default_admin` succeeds
- `docker compose exec backend pytest` passes all smoke tests
- DRF browsable API accessible at http://localhost:8000/api/
</verification>

<success_criteria>
Django 5 runs in Docker with DRF configured, Celery connected to Redis, health-check endpoint returning 200, and the create_default_admin management command working. Smoke tests pass via pytest.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
