---
phase: 02-authentication-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/core/migrations/0002_update_roles.py
  - backend/config/settings.py
  - backend/core/serializers.py
  - backend/core/management/commands/seed_roles.py
autonomous: true

must_haves:
  truths:
    - "Role table contains exactly 3 roles: Admin, Operations, Finance"
    - "JWT access token includes user role in custom claims"
    - "Passwords are hashed with bcrypt and validated against strength rules (min 8 chars, 1 uppercase, 1 number)"
    - "UserProfile is_force_password_change field exists for admin-reset flow"
  artifacts:
    - path: "backend/core/models.py"
      provides: "Updated Role choices (3 roles), UserProfile with force_password_change flag"
      contains: "ROLE_CHOICES"
    - path: "backend/core/serializers.py"
      provides: "Custom JWT token serializer with role claim"
      contains: "CustomTokenObtainPairSerializer"
    - path: "backend/core/management/commands/seed_roles.py"
      provides: "Management command to seed 3 roles"
      contains: "seed_roles"
    - path: "backend/config/settings.py"
      provides: "Bcrypt hasher priority, password validators, JWT cookie config"
      contains: "bcrypt"
  key_links:
    - from: "backend/core/serializers.py"
      to: "backend/core/models.py"
      via: "UserProfile.role lookup in token claims"
      pattern: "profile\\.role"
    - from: "backend/config/settings.py"
      to: "rest_framework_simplejwt"
      via: "TOKEN_OBTAIN_SERIALIZER override"
      pattern: "TOKEN_OBTAIN_SERIALIZER"
---

<objective>
Configure Django auth backend for 3-role RBAC with bcrypt hashing, custom JWT claims, and password strength enforcement.

Purpose: Establish the server-side auth foundation that all other Phase 2 plans depend on. JWT tokens must carry role information so the frontend can enforce RBAC without extra API calls.
Output: Updated Role model (3 roles), custom JWT serializer with role claims, bcrypt hashing, password validators, seed command.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/models.py
@backend/config/settings.py
@backend/config/urls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Role model to 3 roles, add force_password_change, configure bcrypt</name>
  <files>
    backend/core/models.py
    backend/core/migrations/0002_update_roles.py
    backend/config/settings.py
    backend/core/management/commands/seed_roles.py
  </files>
  <action>
    1. Update `backend/core/models.py`:
       - Change Role.ROLE_CHOICES to exactly 3 roles: Admin, Operations, Finance (remove Manager and Viewer per user decision — 3 roles locked)
       - Remove MANAGER and VIEWER class constants
       - Add `is_force_password_change = models.BooleanField(default=False)` to UserProfile — used when admin force-resets a password
       - Keep all existing fields (phone, department, is_active) unchanged

    2. Create migration `backend/core/migrations/0002_update_roles.py`:
       - RunPython operation: delete Role rows with name in ('Manager', 'Viewer') if they exist
       - AddField: is_force_password_change to UserProfile (default=False)
       - Write as manual migration (Docker CLI not available in exec env)

    3. Update `backend/config/settings.py`:
       - Add `django.contrib.auth.hashers.BCryptSHA256PasswordHasher` as FIRST item in PASSWORD_HASHERS list (requires `bcrypt` pip package)
       - Keep Django default hashers after it as fallbacks
       - Update AUTH_PASSWORD_VALIDATORS to enforce: MinimumLengthValidator with OPTIONS min_length=8, add RegexValidator-based custom validator or use a custom validator class
       - Actually, Django doesn't have built-in uppercase/number validators. Create a custom validator. Add to INSTALLED_APPS or validators list:
         - Create `backend/core/validators.py` with `UppercaseValidator` (at least 1 uppercase) and `NumberValidator` (at least 1 number)
         - Reference them in AUTH_PASSWORD_VALIDATORS
       - Add `bcrypt>=4.1,<5.0` to requirements.txt

    4. Create `backend/core/management/commands/seed_roles.py`:
       - Idempotent command that ensures exactly 3 Role records exist: Admin, Operations, Finance
       - Uses get_or_create for each
       - Deletes any roles not in the set of 3 (cleanup from Phase 1's 5-role seed)
       - Output: "Roles seeded: Admin, Operations, Finance"

    Note: Do NOT add `backend/core/validators.py` to files_modified above — adding it here as part of this task.
  </action>
  <verify>
    - `grep -c "ROLE_CHOICES" backend/core/models.py` shows the 3-role list
    - `grep "BCryptSHA256" backend/config/settings.py` confirms bcrypt is first hasher
    - `grep "is_force_password_change" backend/core/models.py` confirms field exists
    - `cat backend/core/management/commands/seed_roles.py` shows idempotent seed logic
    - `cat backend/core/validators.py` shows uppercase and number validators
  </verify>
  <done>Role model has exactly 3 choices, UserProfile has force_password_change flag, bcrypt is primary hasher, password validators enforce min 8 chars + 1 uppercase + 1 number, seed_roles command exists</done>
</task>

<task type="auto">
  <name>Task 2: Custom JWT serializer with role claims and cookie settings</name>
  <files>
    backend/core/serializers.py
    backend/config/settings.py
    backend/requirements.txt
  </files>
  <action>
    1. Create `backend/core/serializers.py`:
       - `CustomTokenObtainPairSerializer` extending `TokenObtainPairSerializer`:
         - Override `get_token(cls, user)` classmethod
         - Add `token['role']` from `user.profile.role.name` (handle missing profile gracefully — default to empty string)
         - Add `token['username']` = `user.username`
         - Add `token['email']` = `user.email`
         - Add `token['full_name']` = `user.get_full_name()`
         - Add `token['is_force_password_change']` = `user.profile.is_force_password_change` (bool)
       - `CustomTokenObtainPairView` extending `TokenObtainPairView`:
         - Uses `CustomTokenObtainPairSerializer`

    2. Update `backend/config/settings.py` SIMPLE_JWT section:
       - Add `"TOKEN_OBTAIN_SERIALIZER": "core.serializers.CustomTokenObtainPairSerializer"`
       - Add `"AUTH_COOKIE": "access_token"` — for HttpOnly cookie strategy (Plan 02-03 will implement the cookie middleware)
       - Add `"AUTH_COOKIE_SECURE": not DEBUG` — secure in production
       - Add `"AUTH_COOKIE_SAMESITE": "Lax"`
       - Set `"BLACKLIST_AFTER_ROTATION": True` and add `"rest_framework_simplejwt.token_blacklist"` to INSTALLED_APPS for logout token invalidation
       - Set `"UPDATE_LAST_LOGIN": True` to track last login timestamp

    3. Update `backend/requirements.txt`:
       - Add `bcrypt>=4.1,<5.0` (for BCryptSHA256PasswordHasher)

    4. Update `backend/config/urls.py`:
       - Replace the default `TokenObtainPairView` import with `CustomTokenObtainPairView` from core.serializers
       - Keep the same URL path `/api/auth/token/`
  </action>
  <verify>
    - `grep "CustomTokenObtainPairSerializer" backend/core/serializers.py` confirms custom serializer exists
    - `grep "TOKEN_OBTAIN_SERIALIZER" backend/config/settings.py` confirms override
    - `grep "token_blacklist" backend/config/settings.py` confirms blacklist app added
    - `grep "bcrypt" backend/requirements.txt` confirms bcrypt dependency
    - `grep "CustomTokenObtainPairView" backend/config/urls.py` confirms custom view wired
  </verify>
  <done>JWT tokens include role, username, email, full_name, is_force_password_change claims. Token blacklist enabled for logout. Bcrypt in requirements. Custom view wired in urls.py.</done>
</task>

</tasks>

<verification>
- Role model has exactly 3 role choices (Admin, Operations, Finance)
- SIMPLE_JWT config includes TOKEN_OBTAIN_SERIALIZER pointing to custom serializer
- Custom JWT serializer adds role claim to token payload
- Bcrypt is first password hasher in settings
- Password validators enforce 8+ chars, 1 uppercase, 1 number
- seed_roles management command exists and is idempotent
- Token blacklist app is in INSTALLED_APPS
</verification>

<success_criteria>
A JWT token obtained via /api/auth/token/ contains role, username, email, full_name, and is_force_password_change in its payload. Passwords are hashed with bcrypt and validated against strength rules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-rbac/02-01-SUMMARY.md`
</output>
