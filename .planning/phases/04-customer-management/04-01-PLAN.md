---
phase: 04-customer-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/customers/models.py
  - backend/customers/migrations/0002_customer_fields_update.py
autonomous: true

must_haves:
  truths:
    - "Customer model has all fields from CONTEXT.md decisions: company_name, tin, contact_person, phone, email, address, business_type, preferred_port FK, currency_preference FK, credit_terms, notes"
    - "TIN field has a db_index for fast duplicate lookups"
    - "Soft-delete via is_active=False is preserved from Phase 1 model"
    - "preferred_port is a ForeignKey to setup.Port; currency_preference is a ForeignKey to setup.Currency"
  artifacts:
    - path: "backend/customers/models.py"
      provides: "Complete Customer model with all fields"
      contains: "class Customer"
    - path: "backend/customers/migrations/0002_customer_fields_update.py"
      provides: "Migration adding new fields"
  key_links:
    - from: "backend/customers/models.py"
      to: "backend/setup/models.py"
      via: "ForeignKey imports"
      pattern: "models\\.ForeignKey.*Port|models\\.ForeignKey.*Currency"
---

<objective>
Update the Customer Django model with all fields specified in the Phase 4 CONTEXT.md decisions, add ForeignKey relationships to lookup tables (Port, Currency), and generate the migration.

Purpose: The Customer model from Phase 1 is a skeleton. This plan adds the complete field set needed for the customer CRUD workflow.
Output: Updated Customer model + migration file ready to apply.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customer-management/04-CONTEXT.md
@backend/customers/models.py
@backend/setup/models.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Customer model with all fields and FK relationships</name>
  <files>backend/customers/models.py</files>
  <action>
Update the existing Customer model in backend/customers/models.py to include all fields from the CONTEXT.md decisions:

**Existing fields to keep (already present):**
- company_name (CharField, max_length=200, required)
- customer_type (CharField, choices: Company/Individual)
- contact_person (CharField, max_length=150, blank=True)
- email (EmailField, blank=True)
- phone (CharField, max_length=50, blank=True)
- tin (CharField, max_length=50, blank=True, db_index=True)
- address (TextField, blank=True)
- is_active (BooleanField, default=True)
- notes (TextField, blank=True)

**New fields to add:**
- business_type: CharField, max_length=50, blank=True — free text for "importer", "exporter", "freight agent", etc. Do NOT use choices — the CONTEXT says "e.g." which implies free text categories.
- preferred_port: ForeignKey to setup.Port, on_delete=SET_NULL, null=True, blank=True, related_name="customers"
- currency_preference: ForeignKey to setup.Currency, on_delete=SET_NULL, null=True, blank=True, related_name="customers"
- credit_terms: CharField, max_length=50, blank=True — free text for "net 30", "prepaid", etc.

**Keep existing Meta class** with ordering=["company_name"] and indexes on tin, company_name, customer_type, is_active. Add an index on business_type.

Import Port and Currency from setup.models at the top of the file.
  </action>
  <verify>Run `cd backend && python -c "from customers.models import Customer; print([f.name for f in Customer._meta.get_fields()])"` — output must include business_type, preferred_port, currency_preference, credit_terms alongside all existing fields.</verify>
  <done>Customer model has all 13+ fields including FKs to Port and Currency. Existing fields unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and verify the migration</name>
  <files>backend/customers/migrations/0002_customer_fields_update.py</files>
  <action>
Generate the Django migration for the Customer model changes. Since Docker CLI may not be available, create the migration file manually.

The migration should:
1. Depend on ("customers", "0001_initial") and ("setup", "0001_initial") — the setup migration that creates Port and Currency tables
2. Add fields: business_type, preferred_port (FK), currency_preference (FK), credit_terms
3. Add index on business_type

Check the setup migrations directory to find the correct dependency migration name for the setup app. Use `ls backend/setup/migrations/` to find it.

Name the file `0002_customer_fields_update.py`.
  </action>
  <verify>Run `cd backend && python -c "from customers.migrations.0002_customer_fields_update import Migration; print('Dependencies:', Migration.dependencies); print('Operations:', len(Migration.operations))"` — should show correct dependencies and 4-5 operations (4 AddField + 1 AddIndex).</verify>
  <done>Migration file exists, references correct dependencies, and adds all 4 new fields + business_type index.</done>
</task>

</tasks>

<verification>
- `python -c "from customers.models import Customer"` imports without error
- Customer model has ForeignKey to Port and Currency
- Migration file exists at backend/customers/migrations/0002_customer_fields_update.py
- Migration dependencies include both customers.0001_initial and setup initial migration
</verification>

<success_criteria>
Customer model contains all fields from CONTEXT.md decisions. Migration is ready to apply. ForeignKey relationships to Port and Currency lookup tables are established.
</success_criteria>

<output>
After completion, create `.planning/phases/04-customer-management/04-01-SUMMARY.md`
</output>
