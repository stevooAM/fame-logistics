---
phase: 04-customer-management
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(dashboard)/customers/components/CustomerFormDialog.tsx
  - frontend/src/app/(dashboard)/customers/components/TinWarningDialog.tsx
  - frontend/src/app/(dashboard)/customers/page.tsx
autonomous: true

must_haves:
  truths:
    - "A modal form opens when clicking Add Customer, containing all customer fields with validation"
    - "Inline cell editing works directly in the AG Grid row when clicking Edit from kebab menu"
    - "Saving a customer with a duplicate TIN shows a warning dialog naming the existing customer before proceeding"
    - "Phone field accepts local (02xx, 05xx) and international (+233, +44, +1) formats"
    - "TIN field accepts prefix formats: C, P, G, Q, GHA, EUROPE"
    - "Customer type is validated as Company or Individual"
    - "After successful save, the customer list refreshes to show the new/updated record"
  artifacts:
    - path: "frontend/src/app/(dashboard)/customers/components/CustomerFormDialog.tsx"
      provides: "Create/edit modal form with React Hook Form + Zod validation"
      min_lines: 80
    - path: "frontend/src/app/(dashboard)/customers/components/TinWarningDialog.tsx"
      provides: "Duplicate TIN warning dialog"
  key_links:
    - from: "frontend/src/app/(dashboard)/customers/components/CustomerFormDialog.tsx"
      to: "/api/customers/"
      via: "apiFetch POST/PATCH"
      pattern: "apiFetch.*api/customers"
    - from: "frontend/src/app/(dashboard)/customers/components/CustomerFormDialog.tsx"
      to: "/api/customers/check-tin/"
      via: "apiFetch GET for TIN duplicate check"
      pattern: "apiFetch.*check-tin"
---

<objective>
Build the customer create/edit modal form with React Hook Form + Zod validation, inline AG Grid cell editing, duplicate TIN warning dialog, and phone/TIN format validators.

Purpose: CUST-03, CUST-04, CUST-07, CUST-08, CUST-09, CUST-11 — staff need to add and edit customers with proper validation and duplicate detection.
Output: Working create/edit flow with all validation rules enforced.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customer-management/04-CONTEXT.md
@.planning/phases/04-customer-management/04-02-SUMMARY.md
@.planning/phases/04-customer-management/04-04-SUMMARY.md
@frontend/src/types/customer.ts
@frontend/src/lib/api.ts
@frontend/src/app/(dashboard)/customers/page.tsx
@frontend/src/app/(dashboard)/customers/components/CustomerTable.tsx
@frontend/src/app/(dashboard)/admin/users/components/UserFormDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CustomerFormDialog and TinWarningDialog</name>
  <files>
    frontend/src/app/(dashboard)/customers/components/CustomerFormDialog.tsx
    frontend/src/app/(dashboard)/customers/components/TinWarningDialog.tsx
  </files>
  <action>
IMPORTANT: Before writing any UI code, invoke the `/frontend-design` skill to get design guidance for the customer form modal layout, field arrangement, and validation UX. Apply the guidance to all components.

**TinWarningDialog.tsx:**
- shadcn/ui AlertDialog component
- Props: open, onClose, onProceed, existingCustomerName: string
- Message: "A customer with this TIN already exists: {existingCustomerName}. Proceed anyway?"
- Two buttons: "Cancel" (closes dialog), "Proceed Anyway" (calls onProceed)
- Styling: warning amber accent for the proceed button

**CustomerFormDialog.tsx:**
- shadcn/ui Dialog component
- Props: open, onClose, customer (Customer | null for create vs edit mode), onSuccess (callback after save)
- Uses React Hook Form with Zod schema validation

**Zod schema (customerFormSchema):**
- company_name: z.string().min(1, "Company name is required").max(200)
- customer_type: z.enum(["Company", "Individual"])
- tin: z.string().max(50).optional().or(z.literal(""))
  - Custom refinement: if non-empty, must match pattern for TIN prefixes (C, P, G, Q, GHA, EUROPE followed by digits/alphanumeric). Use regex: /^(C|P|G|Q|GHA|EUROPE)[\w\d-]*$/i — but keep it lenient since the CONTEXT says "supports prefix formats" not "strictly validates"
- contact_person: z.string().max(150).optional().or(z.literal(""))
- phone: z.string().max(50).optional().or(z.literal(""))
  - Custom refinement: if non-empty, must match local Ghana formats (02x, 05x, 0xx with 9-10 digits) or international formats (+country code with digits). Regex: /^(\+?\d{1,4}[-\s]?)?\d{6,14}$/ — lenient to accept mixed formats per CUST-09
- email: z.string().email("Invalid email").optional().or(z.literal(""))
- address: z.string().optional().or(z.literal(""))
- business_type: z.string().max(50).optional().or(z.literal(""))
- preferred_port: z.number().nullable().optional()
- currency_preference: z.number().nullable().optional()
- credit_terms: z.string().max(50).optional().or(z.literal(""))
- notes: z.string().optional().or(z.literal(""))

**Form layout:**
- Two-column grid for fields (responsive: 1 col on mobile, 2 on desktop)
- Row 1: Company Name (full width)
- Row 2: Customer Type (select), TIN
- Row 3: Contact Person, Phone
- Row 4: Email (full width)
- Row 5: Address (full width, textarea)
- Row 6: Business Type, Credit Terms
- Row 7: Preferred Port (select from /api/setup/dropdowns/), Currency Preference (select from same endpoint)
- Row 8: Notes (full width, textarea)
- Footer: Cancel + Save buttons

**Port and Currency dropdowns:** Fetch from GET /api/setup/dropdowns/ on dialog open. Use the ports and currencies arrays to populate select options.

**TIN duplicate check flow:**
1. On form submit, if TIN is non-empty, call GET /api/customers/check-tin/?tin={value}&exclude_id={customer.id if editing}
2. If duplicate=true, open TinWarningDialog with the existing customer name
3. If user clicks "Proceed Anyway", continue with the save
4. If user clicks "Cancel", return to the form

**Save flow:**
- Create: POST /api/customers/ with form data via apiFetch
- Edit: PATCH /api/customers/{id}/ with form data via apiFetch
- On success: call onSuccess callback, close dialog
- On error: show field-level errors from API response (DRF returns {field: [errors]})

All API calls use apiFetch<T>.
  </action>
  <verify>Run `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error"` — should be 0 or pre-existing only. Visually: opening Add Customer should show a form dialog with all fields.</verify>
  <done>CustomerFormDialog renders all fields with Zod validation, performs TIN duplicate check before save, and submits via apiFetch. TinWarningDialog shows existing customer name and allows proceeding.</done>
</task>

<task type="auto">
  <name>Task 2: Wire modal and inline editing into customer page</name>
  <files>
    frontend/src/app/(dashboard)/customers/page.tsx
    frontend/src/app/(dashboard)/customers/components/CustomerTable.tsx
  </files>
  <action>
**Update customers/page.tsx:**
- Add state: isFormOpen (boolean), editingCustomer (Customer | null)
- Wire "Add Customer" toolbar button -> setIsFormOpen(true), setEditingCustomer(null)
- Wire kebab "Edit" action -> setEditingCustomer(customer), setIsFormOpen(true)
- Render CustomerFormDialog with open={isFormOpen}, customer={editingCustomer}, onSuccess={() => { setIsFormOpen(false); incrementRefreshTrigger(); }}
- Remove placeholder console.log handlers from 04-04

**Update CustomerTable.tsx for inline cell editing:**
- Enable AG Grid's editable property on the following columns: company_name, tin, contact_person, phone, business_type, credit_terms
- Set `editType: "fullRow"` is NOT needed — use individual cell editing per CONTEXT.md ("Inline cell editing directly in the AG Grid row")
- On cell value change (onCellValueChanged), call PATCH /api/customers/{id}/ with the changed field via apiFetch
- Show a brief success indicator (e.g., cell flash green) or error toast on failure
- The kebab "Edit" action should trigger the modal form (for full edit with all fields including dropdowns). Inline editing is for quick edits on visible columns only.

Per CONTEXT.md (Claude's Discretion): After modal save, close modal and refresh list (do not navigate to detail).
  </action>
  <verify>Visually: clicking "Add Customer" opens modal form. Clicking kebab "Edit" opens modal pre-filled with customer data. Double-clicking a cell in the grid allows inline editing. TypeScript compiles without errors.</verify>
  <done>Add Customer and Edit Customer flows work via modal. Inline cell editing works for visible columns. TIN duplicate check triggers on save. List refreshes after save.</done>
</task>

</tasks>

<verification>
- CustomerFormDialog uses React Hook Form + Zod
- TIN validation allows C, P, G, Q, GHA, EUROPE prefixes
- Phone validation accepts local Ghana and international formats
- TIN duplicate check calls /api/customers/check-tin/ before save
- TinWarningDialog names the existing customer
- Inline cell editing triggers PATCH on the API
- All API calls use apiFetch
</verification>

<success_criteria>
Staff can create customers via modal form with full validation, edit via modal or inline cell editing, and receive duplicate TIN warnings naming the existing customer. Phone and TIN accept all required formats. List refreshes after save.
</success_criteria>

<output>
After completion, create `.planning/phases/04-customer-management/04-05-SUMMARY.md`
</output>
