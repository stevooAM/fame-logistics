---
phase: 04-customer-management
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - frontend/src/app/(dashboard)/customers/[id]/page.tsx
  - frontend/src/app/(dashboard)/customers/[id]/components/CustomerInfoPanel.tsx
  - frontend/src/app/(dashboard)/customers/[id]/components/LinkedJobsPanel.tsx
  - frontend/src/app/(dashboard)/customers/components/CustomerTable.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /customers/[id] shows a two-column layout: customer info on the left, linked jobs on the right"
    - "All customer fields are displayed and editable inline on the detail page (no modal needed)"
    - "The linked jobs panel shows job numbers and status (placeholder data for now — Phase 5 adds real jobs)"
    - "Clicking View Detail in the kebab menu navigates to the detail page"
    - "A back button returns to the customer list"
  artifacts:
    - path: "frontend/src/app/(dashboard)/customers/[id]/page.tsx"
      provides: "Customer detail page with two-column layout"
      min_lines: 30
    - path: "frontend/src/app/(dashboard)/customers/[id]/components/CustomerInfoPanel.tsx"
      provides: "Left panel with all customer fields, inline editable"
      min_lines: 60
    - path: "frontend/src/app/(dashboard)/customers/[id]/components/LinkedJobsPanel.tsx"
      provides: "Right panel with linked jobs list (placeholder for Phase 5)"
  key_links:
    - from: "frontend/src/app/(dashboard)/customers/[id]/page.tsx"
      to: "/api/customers/{id}/"
      via: "apiFetch GET for customer detail"
      pattern: "apiFetch.*api/customers"
    - from: "frontend/src/app/(dashboard)/customers/components/CustomerTable.tsx"
      to: "frontend/src/app/(dashboard)/customers/[id]/page.tsx"
      via: "router.push for View Detail action"
      pattern: "push.*customers/"
---

<objective>
Build the customer detail page with a two-column layout: customer info fields (inline editable) on the left, linked jobs panel on the right.

Purpose: CUST-10 requires customer records to link to their jobs. The detail view is the central hub for viewing and editing a single customer with all their context.
Output: Working detail page at /customers/[id] with inline editing and placeholder jobs panel.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-customer-management/04-CONTEXT.md
@.planning/phases/04-customer-management/04-02-SUMMARY.md
@.planning/phases/04-customer-management/04-04-SUMMARY.md
@.planning/phases/04-customer-management/04-05-SUMMARY.md
@frontend/src/types/customer.ts
@frontend/src/lib/api.ts
@frontend/src/app/(dashboard)/customers/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build customer detail page with two-column layout</name>
  <files>
    frontend/src/app/(dashboard)/customers/[id]/page.tsx
    frontend/src/app/(dashboard)/customers/[id]/components/CustomerInfoPanel.tsx
    frontend/src/app/(dashboard)/customers/[id]/components/LinkedJobsPanel.tsx
  </files>
  <action>
IMPORTANT: Before writing any UI code, invoke the `/frontend-design` skill to get design guidance for the customer detail page layout, two-column arrangement, inline editing UX, and linked jobs panel. Apply the guidance to all components.

**customers/[id]/page.tsx:**
- "use client" directive
- Extract `id` from route params
- Fetch customer detail from GET /api/customers/{id}/ via apiFetch on mount
- Loading state: skeleton layout matching two-column structure
- Error state: "Customer not found" message with back link
- Layout: Two-column grid (left ~60%, right ~40%) on desktop, stacked on mobile
- Back button at top: "Back to Customers" linking to /customers
- Page title: customer company_name
- Renders CustomerInfoPanel (left) and LinkedJobsPanel (right)

**CustomerInfoPanel.tsx:**
- Displays ALL customer fields in a structured form-like layout
- Fields are inline-editable (per CONTEXT.md: "inline editing directly on the page, no modal needed"):
  - Click on a field value to enter edit mode (show input/select)
  - Press Enter or click away to save (PATCH /api/customers/{id}/ via apiFetch)
  - Press Escape to cancel edit
  - Show save indicator (checkmark) and error state
- Field layout (label: value pairs, grouped logically):
  - Company Name, Customer Type
  - TIN, Business Type
  - Contact Person, Phone, Email
  - Address
  - Preferred Port (dropdown from /api/setup/dropdowns/), Currency Preference (dropdown)
  - Credit Terms
  - Notes
  - Created at, Updated at (read-only, formatted dates)
- Apply same Zod validation from 04-05 for phone and TIN fields on inline edit save
- Use the project design tokens: primary teal #1F7A8C, dark navy #2B3E50

**LinkedJobsPanel.tsx:**
- Panel heading: "Linked Jobs"
- For now (Phase 4), show an empty state: "No jobs linked yet. Jobs will appear here once created in the Jobs module."
- Structure the component to accept a `customerId` prop so Phase 5 can wire real job data
- Future-proof: include a commented-out fetch call pattern `// TODO Phase 5: apiFetch<JobListResponse>(\`/api/jobs/?customer=${customerId}\`)`
- Per CONTEXT.md specific idea: include a "View all jobs" link (disabled/placeholder for now)
- Per CONTEXT.md (Claude's Discretion): do NOT include a "Create Job" shortcut yet — Phase 5 will determine if that makes sense

All API calls use apiFetch<T>.
  </action>
  <verify>Run `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error"` — should be 0 or pre-existing only. Visually: navigating to /customers/1 should show two-column layout with customer info and empty jobs panel.</verify>
  <done>Customer detail page renders two-column layout. Left panel shows all fields with inline editing. Right panel shows placeholder for linked jobs. Back button returns to /customers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire View Detail action from customer list</name>
  <files>frontend/src/app/(dashboard)/customers/components/CustomerTable.tsx</files>
  <action>
Update the kebab menu "View Detail" action in CustomerTable.tsx to navigate to /customers/{id} using Next.js router.

Per CONTEXT.md (Claude's Discretion): Also make row click navigate to the detail page. Use `onRowClicked` AG Grid callback. Exclude clicks on the actions column (check if the clicked column is the actions column before navigating).

Import and use `useRouter` from `next/navigation`.
  </action>
  <verify>Visually: clicking "View Detail" in kebab menu navigates to /customers/[id]. Clicking a row (not on the actions column) also navigates to the detail page.</verify>
  <done>View Detail kebab action and row click both navigate to /customers/[id]. Actions column clicks do not trigger navigation.</done>
</task>

</tasks>

<verification>
- /customers/[id] page loads customer data from API
- Two-column layout: info left, jobs right
- Inline editing on detail page saves via PATCH
- Back button navigates to /customers
- View Detail from list navigates to detail page
- Row click navigates to detail page (except actions column)
- All API calls use apiFetch
</verification>

<success_criteria>
Customer detail page at /customers/[id] shows two-column layout with inline-editable customer info and placeholder linked jobs panel. Navigation from list to detail works via kebab menu and row click. Inline editing saves changes via API.
</success_criteria>

<output>
After completion, create `.planning/phases/04-customer-management/04-06-SUMMARY.md`
</output>
