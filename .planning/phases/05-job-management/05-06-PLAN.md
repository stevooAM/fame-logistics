---
phase: 05-job-management
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - frontend/src/app/(dashboard)/jobs/[id]/page.tsx
  - frontend/src/app/(dashboard)/jobs/[id]/components/JobDetailHeader.tsx
  - frontend/src/app/(dashboard)/jobs/[id]/components/StatusTransitionDropdown.tsx
  - frontend/src/app/(dashboard)/jobs/[id]/components/DocumentPanel.tsx
  - frontend/src/app/(dashboard)/jobs/[id]/components/AuditTrailTimeline.tsx
  - frontend/src/app/(dashboard)/jobs/[id]/components/ConfirmStatusDialog.tsx
autonomous: true

must_haves:
  truths:
    - "Job detail page renders at /jobs/{id} with all job fields"
    - "Status can be changed via dropdown with confirmation dialog on every transition"
    - "Status reversal shows error for non-Admin users"
    - "Documents can be uploaded with required document type selection"
    - "Documents are listed with download links (presigned URLs)"
    - "Audit trail timeline shows all status changes with user and timestamp"
    - "Submit for Approval appears as a status transition option in the dropdown"
  artifacts:
    - path: "frontend/src/app/(dashboard)/jobs/[id]/page.tsx"
      provides: "Job detail page with all sections"
      min_lines: 50
    - path: "frontend/src/app/(dashboard)/jobs/[id]/components/StatusTransitionDropdown.tsx"
      provides: "Status dropdown with role-aware transition options"
    - path: "frontend/src/app/(dashboard)/jobs/[id]/components/DocumentPanel.tsx"
      provides: "Document upload and list panel"
    - path: "frontend/src/app/(dashboard)/jobs/[id]/components/AuditTrailTimeline.tsx"
      provides: "Chronological audit trail display"
    - path: "frontend/src/app/(dashboard)/jobs/[id]/components/ConfirmStatusDialog.tsx"
      provides: "Confirmation dialog for status transitions"
  key_links:
    - from: "frontend/src/app/(dashboard)/jobs/[id]/page.tsx"
      to: "/api/jobs/{id}/"
      via: "apiFetch GET"
      pattern: "apiFetch.*api/jobs"
    - from: "frontend/src/app/(dashboard)/jobs/[id]/components/StatusTransitionDropdown.tsx"
      to: "/api/jobs/{id}/transition/"
      via: "apiFetch PATCH"
      pattern: "apiFetch.*transition"
    - from: "frontend/src/app/(dashboard)/jobs/[id]/components/DocumentPanel.tsx"
      to: "/api/jobs/{id}/documents/"
      via: "fetch (multipart upload) and apiFetch GET"
      pattern: "api/jobs.*documents"
---

<objective>
Build the Job detail page with field summary, status transition dropdown (with confirmation dialog on every change), document attachment panel, and audit trail timeline.

Purpose: JOB-06 (full detail view), JOB-07 (status workflow via UI), JOB-08 (submit for approval as status transition), JOB-10 (audit trail display), JOB-11 (document attachment UI). This is the most complex frontend page — it ties together all job capabilities.

Output: Complete /jobs/{id} detail page with all interactive sections.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-job-management/05-CONTEXT.md
@frontend/src/types/job.ts
@frontend/src/app/(dashboard)/customers/[id]/page.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build status transition dropdown and confirmation dialog</name>
  <files>frontend/src/app/(dashboard)/jobs/[id]/components/StatusTransitionDropdown.tsx, frontend/src/app/(dashboard)/jobs/[id]/components/ConfirmStatusDialog.tsx</files>
  <action>
**ConfirmStatusDialog.tsx:**
- Dialog component (uses Dialog from @/components/ui/dialog)
- Props: open, onClose, currentStatus: JobStatus, newStatus: JobStatus, onConfirm: () => void, isLoading: boolean
- Shows: "Are you sure you want to change the status from {currentLabel} to {newLabel}?"
- Uses StatusBadge to show both old and new status visually (arrow between them)
- Cancel and Confirm buttons. Confirm shows loading state.
- LOCKED DECISION: Every status change requires this confirmation dialog — no exceptions.

**StatusTransitionDropdown.tsx:**
- Props: job: Job, userRole: string, onStatusChange: (job: Job) => void
- Renders a dropdown (custom built — same pattern as export dropdown from 04-07, using fixed-inset overlay since shadcn DropdownMenu is not available)
- Current status shown as a StatusBadge that acts as the dropdown trigger
- Dropdown options: only valid transitions from current status
- Transition rules (must match backend exactly):
  - DRAFT: can go to PENDING, CANCELLED
  - PENDING: can go to IN_PROGRESS, CANCELLED (and this IS the "Submit for Approval" transition — label it "Submit for Approval / In Progress" or just show PENDING->IN_PROGRESS and note that submitting for approval happens when moving to PENDING)
  - Actually per CONTEXT: "Submit for Approval" is a status transition in the status dropdown. The submit-for-approval action happens when status goes from DRAFT to PENDING. Label the DRAFT->PENDING option as "Submit for Review" or keep it as "Pending" — the backend handles creating the approval queue entry.
  - IN_PROGRESS: can go to CUSTOMS, CANCELLED
  - CUSTOMS: can go to DELIVERED, CANCELLED
  - DELIVERED: can go to CLOSED
  - CLOSED: no forward transitions
  - CANCELLED: no forward transitions
- **Reversal options**: If userRole is "admin", show backward transitions too (e.g., PENDING->DRAFT, IN_PROGRESS->PENDING, etc.). If userRole is "operations", backward options are NOT shown (backend will reject anyway, but don't show them).
- **Finance role**: If userRole is "finance", the dropdown is disabled (view-only)
- On selecting a transition: open ConfirmStatusDialog
- On confirm: PATCH /api/jobs/{id}/transition/ with { new_status: selectedStatus }
- On success: call onStatusChange with updated job
- On 403 error: show inline error "Only Admin can reverse job status"
- On other errors: show inline error message from API response
  </action>
  <verify>Read both components. Confirm StatusTransitionDropdown renders valid transitions based on current status and user role. Confirm ConfirmStatusDialog blocks every transition with a confirmation prompt.</verify>
  <done>Status can be changed via dropdown with mandatory confirmation. Transitions respect role rules (Admin sees reversal options, Operations sees forward-only, Finance is disabled). PATCH to /transition/ endpoint handles the change.</done>
</task>

<task type="auto">
  <name>Task 2: Build document panel, audit trail timeline, and job detail page</name>
  <files>frontend/src/app/(dashboard)/jobs/[id]/components/DocumentPanel.tsx, frontend/src/app/(dashboard)/jobs/[id]/components/AuditTrailTimeline.tsx, frontend/src/app/(dashboard)/jobs/[id]/components/JobDetailHeader.tsx, frontend/src/app/(dashboard)/jobs/[id]/page.tsx</files>
  <action>
**DocumentPanel.tsx:**
- Props: jobId: number, userRole: string
- Fetches documents from GET /api/jobs/{jobId}/documents/ via apiFetch on mount
- Displays documents as a list (Claude's discretion — standard list format per CONTEXT):
  - Each row: file icon, file_name, document_type_name badge, file_size formatted (KB/MB), uploaded_by_name, created_at formatted, Download button (opens presigned URL in new tab), Delete button (Admin/Operations only)
- Upload section at top (visible to Admin/Operations only):
  - "Upload Document" button that reveals: file input (any type accepted — LOCKED), document_type dropdown (fetches DocumentType lookup from GET /api/setup/dropdowns/ — use the existing dropdown endpoint from Phase 3), Upload button
  - On upload: POST /api/jobs/{jobId}/documents/upload/ as multipart/form-data (NOT JSON — use FormData). Set Content-Type to undefined/let browser set it (do NOT set application/json for file upload). Use raw fetch with credentials: "include" instead of apiFetch for multipart upload.
  - Show upload progress or loading spinner
  - On success: refresh document list
  - Show "Maximum 20 documents" warning when at limit
- Delete: confirm dialog, then DELETE /api/jobs/{jobId}/documents/{docId}/, refresh list
- Empty state: "No documents attached"

**AuditTrailTimeline.tsx:**
- Props: jobId: number
- Fetches audit trail from GET /api/jobs/{jobId}/audit-trail/ via apiFetch on mount
- Renders a vertical timeline (CSS-only, no library needed):
  - Each entry: circle dot on left, vertical line connecting dots
  - Content: action description (e.g., "Status changed from Draft to Pending"), user_name, relative timestamp (e.g., "2 hours ago") with full date on hover/title attribute
  - Status changes show old->new with StatusBadge components
  - Document upload/delete actions show filename
- Ordered newest first (API returns this order)
- Empty state: "No activity recorded"

**JobDetailHeader.tsx:**
- Props: job: Job
- Shows job_number as large title, customer name as subtitle link (to /customers/{id}), created_at formatted, created_by name
- Job type badge
- Back button (arrow left icon, navigates to /jobs)

**page.tsx (Job detail page):**
- "use client" component
- Route: /jobs/[id]
- Fetches job detail from GET /api/jobs/{id}/ via apiFetch on mount (use the `id` from params — Next.js 15+ async params pattern: `const params = await props.params; const id = params.id;`)
- Layout: two-column on large screens (lg:grid-cols-3), single column on mobile
  - Left column (lg:col-span-2):
    - JobDetailHeader
    - Field summary in FieldGroup sections (reuse FieldGroup pattern from 04-05):
      - "Shipment Details": origin, destination, cargo_description, BL, container_number
      - "Measurements & Cost": weight_kg (formatted with unit "kg"), volume_cbm (formatted with unit "CBM"), total_cost (formatted as currency)
      - "Schedule": eta, delivery_date, assigned_to_name
      - "Notes": notes (or "No notes" if empty)
    - Simple field editing: "Edit" button at top that opens JobFormDialog in edit mode
  - Right column (lg:col-span-1):
    - StatusTransitionDropdown (prominent at top of right column)
    - DocumentPanel
    - AuditTrailTimeline
- Loading state: skeleton placeholders while fetching
- Error state: "Job not found" with back link
- Get user role from AuthContext/useAuth hook for passing to StatusTransitionDropdown and DocumentPanel
  </action>
  <verify>Read all four component files. Confirm page.tsx fetches job by ID, renders field summary in sections, and includes StatusTransitionDropdown, DocumentPanel, and AuditTrailTimeline. Confirm DocumentPanel handles multipart upload. Confirm AuditTrailTimeline renders timeline UI.</verify>
  <done>Job detail page at /jobs/{id} shows complete field summary, status dropdown with confirmation, document upload/download/delete panel, and audit trail timeline. Two-column responsive layout. Edit button opens JobFormDialog. All role-based visibility enforced (Finance view-only, Operations forward-only, Admin full control).</done>
</task>

</tasks>

<verification>
- /jobs/{id} page renders with all job fields in organized sections
- StatusTransitionDropdown shows valid transitions and blocks every change with ConfirmStatusDialog
- Finance users see disabled status dropdown
- Operations users see forward-only transitions
- Admin users see forward and backward transitions
- DocumentPanel uploads files via multipart/form-data with required document_type
- DocumentPanel shows download links using presigned URLs
- DocumentPanel enforces 20-file maximum
- AuditTrailTimeline shows chronological status changes with user and timestamp
- Edit button opens JobFormDialog in edit mode
</verification>

<success_criteria>
- JOB-06 (full detail view) complete
- JOB-07 (status workflow UI with confirmation) complete — every transition requires confirmation per LOCKED decision
- JOB-08 (submit for approval as status transition) complete — it's a status transition in the dropdown per LOCKED decision
- JOB-10 (audit trail display) complete
- JOB-11 (document attachment UI) complete — any file type, max 20, document type required from lookup table
- Status reversal is Admin-only in the UI (matches backend enforcement)
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-management/05-06-SUMMARY.md`
</output>
