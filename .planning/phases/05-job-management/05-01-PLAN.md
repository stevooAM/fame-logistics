---
phase: 05-job-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/jobs/models.py
  - backend/jobs/migrations/0002_add_assigned_eta_delivery.py
autonomous: true

must_haves:
  truths:
    - "Job model has assigned_to, eta, and delivery_date fields"
    - "Auto-number generation produces FMS-{YEAR}-{SEQUENCE} format"
    - "Migration applies cleanly on top of 0001_initial"
  artifacts:
    - path: "backend/jobs/models.py"
      provides: "Updated Job model with all required fields"
      contains: "assigned_to"
    - path: "backend/jobs/migrations/0002_add_assigned_eta_delivery.py"
      provides: "Schema migration for new fields"
  key_links:
    - from: "backend/jobs/models.py"
      to: "django.contrib.auth.models.User"
      via: "ForeignKey assigned_to"
      pattern: "assigned_to.*ForeignKey"
---

<objective>
Update the Job model with missing fields required by the phase context (assigned_to staff, ETA, delivery_date) and implement the auto-number generation logic that produces `FMS-{YEAR}-{SEQUENCE}` job numbers.

Purpose: The existing Job model from Phase 1 has the core fields but is missing assigned_to (optional staff assignment), eta (DateField), and delivery_date (DateField) that the CONTEXT decisions require for the AG Grid columns and form. Auto-number generation is a core requirement (JOB-02).

Output: Updated model + migration file ready for the API layer.
</objective>

<execution_context>
@/Users/steveadjei-mensah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steveadjei-mensah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/jobs/models.py
@backend/jobs/migrations/0001_initial.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing fields to Job model and implement auto-number generation</name>
  <files>backend/jobs/models.py</files>
  <action>
Update the Job model in backend/jobs/models.py to add three new fields:

1. `assigned_to` — ForeignKey to django.contrib.auth.models.User, on_delete=SET_NULL, null=True, blank=True, related_name="assigned_jobs". This is the optional staff assignment field.

2. `eta` — DateField, null=True, blank=True. Expected time of arrival.

3. `delivery_date` — DateField, null=True, blank=True. Actual delivery date.

Add a `generate_job_number()` classmethod or module-level function that:
- Queries the latest Job by job_number descending (filtered to the current year prefix `FMS-{YEAR}-`)
- Extracts the sequence number, increments by 1
- If no jobs exist for the current year, starts at 1
- Returns formatted string: `FMS-{YEAR}-{SEQUENCE:05d}` (e.g., `FMS-2026-00001`)
- Uses `select_for_update()` or `F()` expressions to avoid race conditions in concurrent creation

Override `save()` on Job to auto-assign job_number on creation (when self.pk is None and self.job_number is empty). Call `generate_job_number()` inside save.

Add indexes for the new fields:
- `models.Index(fields=["assigned_to"], name="jobs_job_assigned_idx")`
- `models.Index(fields=["eta"], name="jobs_job_eta_idx")`
- `models.Index(fields=["delivery_date"], name="jobs_job_delivery_idx")`
  </action>
  <verify>Run `python -c "from jobs.models import Job; print(Job._meta.get_fields())"` and confirm assigned_to, eta, delivery_date appear. Verify generate_job_number exists as a callable.</verify>
  <done>Job model has assigned_to (FK User, optional), eta (DateField, optional), delivery_date (DateField, optional), and a generate_job_number function that returns FMS-YYYY-NNNNN format strings.</done>
</task>

<task type="auto">
  <name>Task 2: Create migration for new fields</name>
  <files>backend/jobs/migrations/0002_add_assigned_eta_delivery.py</files>
  <action>
Create the migration file `backend/jobs/migrations/0002_add_assigned_eta_delivery.py` manually (Docker CLI not available in execution environment — established pattern from Phase 1).

The migration must:
- Depend on `("jobs", "0001_initial")` and `migrations.swappable_dependency(settings.AUTH_USER_MODEL)`
- Add `assigned_to` ForeignKey field to Job (null=True, blank=True, on_delete=SET_NULL, related_name="assigned_jobs")
- Add `eta` DateField to Job (null=True, blank=True)
- Add `delivery_date` DateField to Job (null=True, blank=True)
- Add indexes for all three new fields with names matching the model Meta

Use `migrations.AddField` for each field and `migrations.AddIndex` for each index.
  </action>
  <verify>Read the migration file and confirm it has the correct dependency chain, all three AddField operations, and all three AddIndex operations.</verify>
  <done>Migration 0002 exists, depends on 0001_initial, adds assigned_to/eta/delivery_date fields and their indexes to the Job table.</done>
</task>

</tasks>

<verification>
- backend/jobs/models.py contains assigned_to, eta, delivery_date fields
- backend/jobs/models.py contains generate_job_number function returning FMS-YYYY-NNNNN format
- backend/jobs/migrations/0002_add_assigned_eta_delivery.py exists and has correct dependencies
- Job.save() auto-assigns job_number on creation when not provided
</verification>

<success_criteria>
- Job model has all fields from JOB-04 (origin, destination, cargo_description, bill_of_lading, container_number, weight_kg, volume_cbm, total_cost, notes) plus the new assigned_to, eta, delivery_date
- Auto-number generation follows FMS-{YEAR}-{SEQUENCE:05d} format per JOB-02
- Migration applies cleanly after 0001_initial
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-management/05-01-SUMMARY.md`
</output>
